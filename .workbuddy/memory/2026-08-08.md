# 2026-08-08

## LeftAvatar 弹出层"上边绿框"与退出按钮等宽优化
- 用户诉求：上方绿框（InfoPopover 顶部 dispersion-bg 散景背景横幅，品牌青绿 #13987f）宽度要与下方"退出登录"按钮一致。
- 根因：InfoPopover 根节点是 `size-fit`（fit-content），按内容收缩后比外层 `n-flex min-w-[284px]` 容器窄；而退出按钮 `w-full` 撑满容器 → 绿框比按钮窄。
- 修复（src/layout/left/components/LeftAvatar.vue）：给 `<InfoPopover>` 加 `class="!w-full"` 强制撑满容器；退出按钮容器 `px-8px → px-10px`，与 InfoPopover 内容内边距(10px)对齐。其余按钮逻辑未动。
- 验证：LeftAvatar.test.ts 2/2 通过；biome clean；vue-tsc --noEmit 0 error。预览图 leftavatar-popover-preview.html 已补绿色横幅并等宽对齐。
- 备注：经核实 `--avatar-border-color` 是面板色（白），"绿框"实为 InfoPopover 顶部青绿散景背景图，非头像边框。

## 消息界面原型对齐优化方案（生成）
- 依据：docs/TJG-prototype.html「消息视图」四栏原型 vs 项目实际组件（见 Explore agent 对比报告）。范围=仅布局与功能，忽略色彩。
- 已核实：MessageSessionToolbar filterOptions(116-120) 仅 all/unread/mention；群聊/单人 sessionTypeFilter 已定义且 useMessageSessionFilters 有逻辑但无按钮。ChatMain:329 canSetSticky 硬编码 false(TODO)。ReplyComposer.vue / PinnedMessageBanner.vue 已写未引用。ChatHeaderRoot 的 voice/video/screenShare handler 仅 logger.info 空壳。
- 方案分三级：P0(6项低成本接线/空壳: 群聊单人过滤、置顶可设置、回复组件接线、收藏置顶接线、通话按钮接线、批量保存到电脑) / P1(7项功能补齐: 联邦图标、房间内搜索、批量复制、更多折叠、成员在线离线分组、收藏面板、小部件) / P2(2项架构差异: 通话浮层vs独立窗口、房间内全文检索服务层)。每条含原型/现状/改造/影响文件/工作量。
- 交付：用户确认后已落盘 `docs/消息界面原型对齐优化方案-2026-08-08.md`（沿用既有 `docs/原型对齐优化方案-*.md` 约定）。

## 消息界面对齐（首批 5 项 #1/#7/#9/#5/#3 已落地 + 新增 #16）
- 全部走 TDD（red→green→refactor）；验收：vue-tsc 本次涉及文件 0 error（全局仅剩 1 个 pre-existing `SecurityKeySetupDialog.vue` 存量错误）、biome clean、相关 vitest 46 项全绿。
- #1 工具栏补「群聊/单人」type 过滤按钮（`MessageSessionToolbar.vue` + `locales/*/space.json`）。
- #7 头部联邦 globe 图标（`ChatHeaderRoot`/`ChatHeaderInfo` 按 `extractServerName` 比较 room vs 本地 server；`userIdentity.ts` 导出 `extractServerName`）。
- #9 批量栏「复制」按钮（`ChatMsgMultiChoose.vue` 用 `navigator.clipboard.writeText`；复用 `useMessageMultiSelect` 风格）。
- #5 通话按钮接真实窗口（`useWindow().startRtcCall(AUDIO/VIDEO)`；屏幕共享复用视频通话窗口并加注释）。
- #3 ReplyComposer 接线：移除 `#replyDiv` DOM 注入（useEditorDom 删 REPLY 分支+createReplyDom、useMsgInputSend/useMsgInput 剥离逻辑、useMsgInputEvents REPLY 分支），数据保留在 `reply` ref；`MsgInput.vue` 渲染 `ReplyComposer`。
- #16 右侧信息面板折叠（本会话新增需求）：群聊默认 `isCollapsed=true`（平时折叠），折叠把手 `opacity-100` 常显 + `role/aria-expanded/aria-label`；好友/单聊保持 `w-0` 无面板与把手；`locales/*/home.json` 加 `chat_sidebar.toggle.expand/collapse`。新增 `ChatSidebar.test.ts`（3 绿）。
- 关键踩坑：① naive-ui 组件 NButton 在 vitest 必须 stub 成真实 `<button>` 以保留点击/属性；② `vi.hoisted` 返回值里的 mock 变量必须同步加到顶部解构（否则 `ReferenceError`）；③ vue-test-utils `isVisible()` 对 `v-show` 的 `display:none` 判定不稳定，改判 `attributes('style')` 含 `display: none`；④ `MatrixClientService` mock 需同时提供 `matrixClientService` 命名导出与 `default`（因 `MatrixSpecialFriendService` 用 default import 且在模块加载时实例化）。
- 计划文档已追加「六、实施进展」小节记录上述落地项；待办 #2/#6/#4/#12/#8/#11/#10/#13/P2。

## 项目冗余全面审查（4 维度并行 Explore）
- 维度：① 工具函数重复 ② 死代码/未使用 ③ 设计令牌/i18n/样式 ④ 组件/服务/Store 结构。
- 高价值发现：
  - **死代码**：21 个 Vue 组件全仓零引用（约 3800 行，含 `performance/` 整目录）；`useMessageStore`(stores/domains/chat/message.ts 84行) 自述废弃仅自身测试引用；`text.ts:33-37` 残留 `#replyDiv` 分支（已无代码创建该节点）；6 个 `_Matrix*` 私有常量零用；10 个空目录。
  - **工具函数重复**：文件大小格式化 7 份（`Formatting.formatBytes` 基准）、`Validate.ts` 与 `inputValidation.ts` 双校验体系（规则冲突，Validate.ts 10 个导出死代码）、HTML 转义 4 份（字符集不一、`Validate.ts` 漏 `&` 安全隐患）、`formatDuration` 桌面/移动分叉已致 BUG（移动端缺 `Math.floor`）、剪贴板复制 8 处无封装。
  - **设计令牌**：`--color-*`/`--bg-*` 旧别名残留 10 处（与"迁移完毕"说法矛盾）；`design-tokens.css` 140 个 token 零引用（36%）；`:root` 14 个同名重复定义 7 个值冲突（`--font-family` 被裸 'PingFang' 覆盖成 BUG）；三套排版令牌并行（`--tjg-font-size-*`/`--font-size-*`/`typography.scss --text-*`）；`--hula-*` 21 处引用但全库零定义（静默失效）。
  - **i18n**：跨命名空间重复文案 1729 条（27%）；单文件内自我重复 zh 619/en 583；989 个 key（15%）严格判定未被引用（需复核动态拼接）。
  - **组件/服务结构**：ThreadView 桌面/移动 276 行逐字重复；加密三 flow composable 仅移动端用、桌面 3 大 Dialog 重写（约 1500+ 行重复）；room 四 facade(Query/Member/Read/Action) 零逻辑透传；DM 创建 4 处并存；Room/User 举报 Dialog 克隆；好友三 service、两个 SummaryService 职责模糊。
- 用户已要求把清单落盘：`docs/项目冗余审查-2026-08-08.md`（带 `[ ]` 勾选框，按 P0~P4 优先级分组，注明零引用组件清单与删除安全性）。建议后续按"正确性安全 → 用户可见 BUG → 高收益删死代码 → 合并结构"顺序处置；可跑 `pnpm knip`（需先在 knip.config.ts 的 ignore 加 `src/typings/components*.d.ts` 才能让未用组件复现）。
- 审查工作流（并行 4 个 Explore 子代理按维度）已沉淀为可复用经验。

## 修 P0 正确性/安全 Bug（来自冗余审查清单）
- HTML 转义漏 `&`：新建 `src/utils/escapeHtml.ts`（规范唯一实现，`&→<→>→"→'` 顺序），`Validate.ts` 的 `sanitizeInput`（`&amp;` 入口/XSS）、`inputValidation.ts` 的 `sanitizeHtml`、`FriendListItem.vue`、`src/workers/highlightTask.ts` 的局部 `escapeHtml` 全部改走它。原 `sanitizeInput` 还漏 `/`、`&#x27;` 写法不一致等问题一并消除。
- `formatDuration` 移动端 BUG：`src/mobile/views/my/BurnAfterReadSettings.vue` 缺 `Math.floor`（90 秒显示「1.5 分钟」）→ 已对齐桌面版。
- `--font-family` 覆盖 BUG：`design-tokens.css:374` 裸 `--font-family: 'PingFang';` 覆盖 L316 的 `var(--tjg-font-family)`（丢 `-apple-system` 回退）→ 已删除该覆盖行。
- `--hula-*` 死前缀：`model.tsx` 21 处引用全库零定义 → 在 `:root` 补 10 个 `--hula-*` 别名映射到 `--tjg-*`，亮/暗/高对比自动生效。
- 验证：biome clean（7 改动文件）；vue-tsc --noEmit 改动文件 0 error（仅存量 `SecurityKeySetupDialog.vue`/`MatrixAccountService.ts` 与本次无关）；vitest：Validate 31 + inputValidation 21 + FriendListItem 5 + BurnAfterReadSettings 三件套 53 + css-naming-lint 2 全绿。`docs/项目冗余审查-2026-08-08.md` 对应 P0 项已勾选并注明。
- 未做（留 P1）：双校验体系 `Validate.ts` vs `inputValidation.ts` 的 MXID/Email/URL 正则收敛（较大重构，本轮仅统一了转义子集）。

## 本会话收尾：P1 收口 + 全量提交
- **P1 收口**（用户"接着做 P1 或 P2"→ 选 P1）：`useClipboard` 抽取（9 处剪贴板调用统一走 `hasTauriRuntime()` 分支）+ `Formatting.formatBytes` 统一（删 7 份本地文件大小格式化）。TDD：`useClipboard` 测试 4 项全绿。
- **回归修复（提交前全量 vitest 发现）**：
  - `PathUtil.test.ts` 9 失败：桌面 userData 根目录由只读 `resourceDir()` 改为可写 `appDataDir()`（正确性修复），同步更新测试断言 `/resource→/app-data` 及 `baseDir: Resource→AppData`。
  - `MsgInput.test.ts` 4 失败：`MsgInput.vue` 新增 `reply`（来自 `useMsgInput`）但测试 `useMsgInput` mock 缺 `reply` → 补 `reply: ref({...})`。
  - `SecuritySettings.test.ts` 1 失败：`MatrixClientService` mock 缺 `getClient`（被 `MatrixSpecialFriendService` 模块加载时实例化调用）→ 补 `getClient: vi.fn()`。
- **提交（husky 钩子=lint-staged + 全量 vue-tsc --noEmit；每次提交都过了）**：a61a7313(C1 util) → 76dda5fc(C2 ui/P1) → c0f81d1b(C2b P2 删21零引用组件) → 45dedc0a(C2c 删 RoomReportDialog) → 280300ee(C3 service) → 0f706d4e(C4 core+死composable) → 90d857e8(C5 i18n) → 501acd8f(C6 test) → 48b89f94(C7 docs) → 0b54cb77(C8 tauri) → 35289590(C9 config) → 2a39a806(C10a 已知失败测试格式化) → 6b011246(C10b auto-imports 再生成)。范围 a61a7313..6b011246，共 11 个提交（C1 在 prior 会话），累计 ~150 文件。
- **关键约束**：husky 钩子对每次 commit 跑全量 `vue-tsc`，故任何存量类型错误都会卡住全部提交（曾因 `SecurityKeySetupDialog.vue:284` 的 `TS2352` 卡住 C1，已用 `as unknown as` 修）。
- **未提交（刻意排除）**：`leftavatar-popover-preview.html`、`微信UI对齐优化.html`、`.trae-html-share-packages/`（Trae 分享缓存 zip）—— 均为预览/工具缓存，非项目源码。
- 全量 vitest 末次：仅 4 个已知长败套件（HomeserverDialog/RoomDetailPane/MatrixFriendService/MatrixDelayedEventsService，共 15 例）失败，无新增回归。屏幕共享按用户指示未实现（复用视频通话窗口）。

## TJG-prototype.html 联系人界面布局优化（傍晚）
- **主诉求**：联系人界面列表栏太宽。审查全部 PC 主视图（消息/联系人/房间/空间/机器人）后确认仅联系人视图布局失衡。
- **根因**：`.contacts-view`(列表) 用 `flex:1` 独占弹性空间(≈836px)，`.contacts-detail`(详情) 被挤到固定 300px —— 与消息视图「列表 300px 固定 + 内容弹性」模式完全颠倒。
- **修复**（`docs/TJG-prototype.html`）：①`.contacts-view` flex:1 → `width:300px; flex-shrink:0; border-right:1px solid #000`（对齐 room-list-panel）；②`.contacts-detail` 300px 固定 → `flex:1; min-width:0`（去 border-left，加 `justify-content:center; overflow-y:auto`，资料卡垂直居中）；③`.detail-meta` 加 `max-width:460px` 防宽区过度拉伸；④`.contacts-notice-row` 改 `flex-direction:column`（窄侧栏下好友通知/群组验证卡片纵向堆叠）。
- **附带修复成员面板 undefined bug**：`getRoomMembers` 被定义两次（11007 有参供成员面板 / 11479 无参供@提及弹窗），JS 后定义覆盖先定义 → 成员面板误用无参版（对象缺 `initial/online/role/server`）→ `${m.initial}` 渲染成 "undefined" 且全部误判离线。已将无参版改名 `getMentionMembers()` 并更新调用点 `showMentionPopup`(11523)。修复后成员面板正确显示 成员(12)/在线8/离线4/联邦标签。
- **验证方式**：Chrome 无头截图（`--headless=new`）。技巧——原型是多屏展示页且有 `#authOverlay` 登录覆盖层初始显示，需注入脚本隐藏覆盖层+只保留含 `.pc` 的视口+按 location.hash 调 `switchView(v)` 再截图。改动类名经 grep 确认仅联系人视图复用（`.detail-meta` 亦被 selectContact 动态渲染 14655 复用，同受益者），消息视图零回归。
- 已按用户要求提交：`docs: fix contacts view layout + member panel undefined bug in prototype` (`47d6e499`)。
- **UI 审查任务（已纠正方向）**：用户澄清要「以原型为设计基准，识别 Tjg 项目真实存在的 UI 问题」，而非分析原型文件本身。已对照真实源码核查：①设计 token 文件 `src/styles/css/design-tokens.css` 自带对比度缺陷注释——浅色 `--tjg-bubble-sent-bg:#13987f` 白字 3.6:1、`--tjg-text-tertiary:#909090` 3.19:1、quaternary `#999999` 2.85:1 均不达 AA 4.5:1（仅 AA-large）；success/warning/danger/info 白字徽标多 fail。②原型用废弃 `--bg-*` 别名 + 223 处硬编码 hex，项目已迁移 `--tjg-*`；照原型实现会引入错误 token。③原型主题选择器（dark/light/auto）无全局应用逻辑、且全文仅暗色 `:root`；真实项目有 `html[data-theme='dark']` 暗色块 + 主题 scss，主题能力原型未如实反映。④桌面好友视图 `src/views/homeWindow/FriendsList.vue` 为单列列表、点击 `router.push('friend-details')` 跳独立详情页，与原型「列表+内联详情分栏」结构不同（原型联系人列表 flex:1 过宽 bug 在真实项目不存在，因根本无内联详情面板）。⑤通话按钮：当前工作树 `ChatHeaderToolbar.vue` 已无条件渲染 voice/video 并接 `startRtcCall()`（含早前「不显示」修复），但该批 src 改动处于未提交状态。报告已输出，未改动代码。
- **生成逐界面 UI 优化文档** `docs/UI界面优化方案-2026-08-08.md`：以原型(8 主视图+登录层)为设计基准、对照 `src/` 真实组件地图，逐界面列「布局调整 + 应添加组件」，含全局项(§0 token/对比度/主题空壳/响应式scale)与优先级汇总(§10 P0→P2)及证据截图列表。文件已写出，未提交。
- **修订 UI 优化方案为「布局调整 + 组件清理」聚焦版**：依据 `docs/消息界面原型对齐优化方案-2026-08-08.md`(#1-#23 差异与进展) + `docs/UI界面需求文档.md`(§2.8 三栏布局/§4 响应式/§3 模块结构/§8 状态/§9 可访问性) + `docs/项目冗余审查-2026-08-08.md`(零引用组件/死代码/令牌冗余)。关键修正：①token 前缀三套并存冲突——代码 `--tjg-*`(630处,规范) vs 需求文档 `--hula-*`(160处,过时) vs 原型 `--bg-*`(378处,废弃)；以 `--tjg-*` 为准。②好友视图：需求文档与实际均为「列表→独立详情页」，**非**原型内联分栏，故不应回填内联 master-detail（纠正初版错误）。③管理后台「外部服务」需求文档 §3.10.12 仍在，但项目 P2 已删 `AdminExternalServices.vue`(零引用)，以项目为准不恢复。④冗余清理区分「删死代码」与「写而未接应接线」(ReplyComposer 已接/PinnedMessageBanner 待接)。文件已 overwrite，未提交。
