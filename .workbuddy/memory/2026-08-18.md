# 2026-08-18 工作日志

## MatrixPushService 测试 mock 修复
- **根因**：`getPushRules()` 在源码中通过 `client.getPushManager().getPushRules()` 调用，但测试 mock 把 `getPushRules` 放在 client 上而非 `mockPushManager` 上
- **修复**：将 `getPushRules` 从 client mock 移到 `mockPushManager`；FT-124 测试用例简化为直接 mock `mockPushManager.getPushRules.mockRejectedValue`
- **影响文件**：`MatrixPushService.test.ts`、`MatrixPushService.sdkManager.test.ts`

## VoIP 通话信令致命断点修复
- **发现**：应用代码使用 `client.voipHandler` / `client.getCallHandler()`，但 SDK 中实际属性是 `client.callEventHandler`（属性非方法，calls 为 Map 而非 Record）
- **影响**：initialize() 永远跳过来电监听注册；getCallById 恒返回 undefined → 接听/挂断全部失效
- **修复**：
  - `voipHelpers.ts`: `getCallById` 改用 `client.callEventHandler?.calls?.get(callId)`；`checkVoipAvailability` 改用 `client.callEventHandler`
  - `MatrixVoIPService.ts`: `initialize` 改用 `client.callEventHandler`
  - `matrix-js-sdk-augmentations.d.ts`: 移除幻影声明 `voipHandler` / `getCallHandler()` / `VoIPHandler`
  - `voipTypes.ts`: 移除 `VoIPCallHandler` 类型
  - 全部测试 mock 同步更新

## 第三轮「设置页大量重复界面」（约 19:55）
- **截图证据**：6 张常规 tab 上有规则排列的"黑色圆角卡片"，文本为 `<setting-label, setting-desc>`；第 7 张（账户）显示红字 `client.mxcUrlToHttp is not a function`，左下角 38% 横向滚动条
- **静态分析**：tabs/*.vue 18 个文件零重复；`.setting-info` SCSS 仅 `display:flex;flex-direction:column` 无 background；全局 CSS 无 setting-* 命中；SDK v40 `client.mxcUrlToHttp` 在 d.ts 仍存在
- **本轮修复**（5 文件）：
  - `MatrixMediaService.getMediaUrl/getThumbnailUrl` 加 try/catch + `typeof client.mxcUrlToHttp !== 'function'` 防御回落
  - `AccountSettings.displayAvatarUrl` try/catch 防整页被 OnErrorCaptured 抓红屏
  - `SettingsContent`：`watch(activeTab)` 主动清理残留 tooltip DOM（`.n-tooltip/[role=tooltip]/.n-popover`）；body `overflow-x:hidden`；暴露 `inspectOverlayNodes()` + `devOutline`
  - `SettingsPage` 加 dev 诊断面板（仅 `import.meta.env.DEV`）：右下角浮「开启 outline」「扫描残留 tooltip」按钮，输出 selectortag/rect/bg/zIndex
- **验证**：`vue-tsc --noEmit` 0 errors · `biome check` clean · 3 个设置页测试套件 43/43
- **下一轮**：让用户重启 dev 后点击「扫描残留 tooltip」，看输出 `selector` 与 `bg` 字段就能锁定黑气泡组件
- **遗留**：C(callId 传递)、D(移动端死路由)、E(信令桩)、F(双信令系统)、H(通话记录)

## 优化计划核查（TJG-optimization-plan-2026-08-18.md）
- 7/8 issues DONE，Issue 4（通话UI）PARTIALLY DONE
- Issue 4 方案文档已过时：项目已有 CallView.vue + callWindow 组件群，不需要新建 voice/ overlay

## SDK 知识点
- matrix-js-sdk v40.2.0 的 VoIP 属性名：`client.callEventHandler`（CallEventHandler 实例）
- `CallEventHandler.calls` 是 `Map<string, MatrixCall>`，不是 Record
- `createCall(roomId: string)` 只接受 roomId，多余参数被忽略
- SDK 自身 client.d.ts 已声明 `callEventHandler?: CallEventHandler`，无需在 augmentation 中重复声明

## Settings 面板空白 + 不能返回（Issue 8 第二轮）
- `defineAsyncComponent` 的 `errorComponent` **只捕获 loader 失败**（动态 import reject），组件加载后 setup/render 抛错不会触发，整面板会静默空白
- 修复：SettingsContent 加 `onErrorCaptured` 钩子 + `renderError` ref；模板用 `v-if="!renderError"` 切换 `<component :is>` 与 `TabErrorFallback`
- 嵌入式（非 standalone）原只有圆 X 关闭按钮（standalone 才可见）→ header 加 ghost 风格"返回"按钮（icon `mdi:arrow-left`，emit close），由 SettingsPage 的 `handleClose` 路由处理（`router.back()` 或回退 `/message`）
- vi.mock factory 不能引用顶层 let，需用 `vi.hoisted(() => ({...}))` 容器
- new SettingsContent test 4 用例：返回按钮、关闭按钮、render-time 异常触发红色兜底（含 tab 名 + 错误信息）、切 tab 清错。biome: 删除 `console.error` + `// eslint-disable-next-line` 注释，改为仅模板渲染
- 测试 mock TS：room mock 上挂自定义 vi.fn 需透传类型（用 `createRoom({ joinedMembers: [...] })` 走默认返回值）

## 八大问题深度排查（续）— Issue 3 根因修复
- **上下文**：`docs/问题深度排查-2026-08-18.md` 已定位 8 问题，上一轮落地 5 处修复（Issue 1/2/6/7/8），Issue 3/4/5 待处理。
- **Issue 3 真正根因（已确认并修复）**：`openMsgSession` → `getSessionDetailWithFriends` → `getDirectSessionDetail(uid)` 完全依赖 `DirectMessageManager`：
  - `getDmForUser(uid, false)` 在 manager 未就绪时静默返 `null`
  - 随后 `createDm(uid)` 内部 `requireManager()` 抛错 → 被 `getSessionDetailWithFriends` try/catch 捕获 → `return null` → `openMsgSession` 弹 toast 后直接 return，**不跳转**
- **修复**：`getDirectSessionDetail` 改为「本地优先 + manager 兜底」——新增 `findLocalDirectRoom(userId)` 扫描 `client.getRooms()` 中"成员数===2 且另一成员即目标用户、非空间"的已同步房间；找到即 `buildSessionFromRoom` 返回并跳转，完全不依赖 DM manager。`createDm` 抛错时再兜底一次本地定位。
- **回归测试**：`MatrixSessionService.test.ts` 新增「manager 无映射且 createDm 抛错时仍从本地 1:1 房间解析、不调用 createDm」用例。
- **Issue 4 状态更新**：工作区中 `src/composables/webrtc/*` 已删、`useVoIPCallFlow` 迁顶层、`src/views/callWindow/*` 新建（useCallMedia/useCallControls/useCallState + 组件群）、`MatrixVoIPService` API 完整 → 通话 UI 重建进行中，按钮→窗口→CallView 骨架已通，剩余媒体流/信令接通。
- **验证**：`MatrixSessionService.test.ts` 6/6、`RoomCardItem.test.ts` 24/24、user 测试 37/37、`SpaceChildrenPane.test.ts` 17/17；biome 单文件 clean。
- **改动文件**：`MatrixSessionService.ts`（getDirectSessionDetail + findLocalDirectRoom）、`__tests__/MatrixSessionService.test.ts`；报告 `docs/问题深度排查-2026-08-18.md` 同步更新。

## Issue 4 媒体层接通（独立立项收尾）
- **用户指令**：Issue 4 媒体层接通作为独立立项收尾；Issue 5 deviceId 收敛并入 MatrixClient 重复初始化专项。
- **Issue 4 真正根因（SDK v40 API 错配）**：`MatrixVoIPService` 沿用旧/自定义 `MatrixCall` 媒体 API，与 vendored `matrix-js-sdk` v40.2.0 真实契约不符：
  - `placeCall(audio: boolean, video: boolean)` / `answer(audio?: boolean, video?: boolean)` 是**布尔优先**，代码却传 `this.localStream`（MediaStream）当 `audio` 布尔 → SDK 仍自行 getUserMedia，与预览流重复采集，且发送流≠预览流
  - 音频静音方法名是 `setMicrophoneMuted(muted)`（非 `setLocalAudioMuted`），视频是 `setLocalVideoMuted(muted)`；旧代码操作 `this.localStream` 轨道只静音/关预览，对方仍可见可闻
  - `CallFeed.isLocal(): boolean` + `CallFeed.stream`；`handleFeedsChanged` 须按 `isLocal()` 拆分本地/远端流
- **修复**：`voipTypes.ts` `VoIPCall` 接口对齐 v40（answer/placeCall 布尔、setMicrophoneMuted、移除 setLocalAudioMuted）；`startCall`/`answerCall` 改 `placeCall(options.audio, options.video)`/`answer(options.audio, options.video)`（不再服务层 getUserMedia）；`handleFeedsChanged` 按 isLocal 拆分；`toggleMute`/`toggleVideo` 改走 `setMicrophoneMuted`/`setLocalVideoMuted` 并返回新状态；移除服务层 `localStream` 字段（cleanup/reset 不再 stop SDK 拥有的轨道）。
- **SDK 契约权威位置**：`node_modules/matrix-js-sdk/lib/web-rtc/call.d.ts`（placeCall L530 / answer L349 / setLocalVideoMuted L411 / setMicrophoneMuted L427）、`callFeed.d.ts`（isLocal L84 / stream L12）。
- **验证**：`vue-tsc --noEmit` 全项目 0 错误；`biome` clean；`MatrixVoIPService.test.ts` 6/6（含 5 个 v40 契约回归用例）；media 目录全量 17 文件 228 用例全绿。
- **新增/改动文件**：`voipTypes.ts`、`MatrixVoIPService.ts`、`__tests__/MatrixVoIPService.test.ts`、`docs/voip-media-layer-收尾-2026-08-18.md`。
- **Issue 5 收口**：确认 `MatrixClient重复初始化收敛-2026-08-09.md` 的修复（shouldReuse 幂等守卫 `MatrixConnectionManager.ts:394`、loginWithToken 前置 resolveStableDeviceId/resolveDeviceIdByWhoami `MatrixClientAuth.ts:256-267`）仍存活、测试全绿；在专项 doc 第九节追加 Issue 5「设备列表过多」收口——根因是重复建 client 累积 deviceId，专项已消除新增，存量历史 device 需服务端/用户清理。

## 设置项「聚拢漂浮」修复（2026-08-18 20:55 → 21:11 → 21:21 二次更正·根因级修复）
- **⚠️ 两次误判复盘**：①初以为 scoped `.setting-item` 缺 `width:100%`（加全局 width:100% 无效）；②改在 `design-tokens.css` 用 `.settings-content-body .setting-item { position:static !important ... }` 兜底补丁（能工作但是 band-aid）。
- **用户提示**：「查看以前的记忆文件，以前这样的问题应该解决过」→ 追到 `ActionList.vue:525` 确有前人写的 `.setting-item { position:static; z-index:auto }` 修复（注释明说覆盖 style.scss 的 menu-item-style(absolute)），但**只作用在 ActionList 自身 scoped 块内**，护住的仅是左导航"更多"菜单 popover，没覆盖设置窗 → 所以是**半成品**。
- **🔴 真正根因（@use-in-scoped 泄漏）**：`style.scss` 的裸 `.setting-item { @include menu-item-style(absolute); @include menu-list() }` 只被 `index.vue`/`LeftAvatar.vue`/`ActionList.vue` 的 `<style scoped>` 用 `@use` 引入。本项目构建下 `@use` 进 scoped 的**外部 partial 未被加 scope 属性**，被编译成**全局规则**，泄漏污染设置窗（`tabs/**`）与 `ChatHeaderSidebar.vue` 的 `.setting-item`。mixin 展开 = `position:absolute; width:fit-content; background; border-radius; box-shadow; z-index:999; white-space:nowrap; overflow:hidden; display:flex; flex-direction:column` → 全变成白色小卡、竖排塌缩、右侧控件被裁（与截图完全吻合）。
- **✅ 根因级修复（替换掉 band-aid，2026-08-18 21:21）**：把 `.setting-item` 规则从共享 `style.scss` **搬进 `ActionList.vue` 自己的 `<style scoped>`**（新增 `@use '@/styles/scss/global/variable.scss' as *` + `@include menu-item-style(absolute)/menu-list()` + `position:static; z-index:auto`），并**删除 `style.scss` 中的裸 `.setting-item`**。全局泄漏消失；设置窗/ChatHeader 的 `.setting-item` 只受各自 scoped 样式控制；原 `design-tokens.css` 的 `!important` 兜底补丁一并移除；`SettingsContent.test.ts` 的 probeFormItems 宽度断言改为结构性不变量（不再保护已删的 CSS 规则）。
- **验证基线**：vue-tsc 0 errors；biome 62 文件 clean；vitest src/views/settingsWindow 19 文件 195 用例全绿。
- **改动文件**：`src/layout/left/components/ActionList.vue`（规则迁入 scoped）、`src/layout/left/style.scss`（删裸 `.setting-item`）、`src/styles/css/design-tokens.css`（删 band-aid）、`src/views/settingsWindow/__tests__/SettingsContent.test.ts`（断言改结构性）。
- **铁律（终版）**：共享 partial 里的裸全局类名经 `@use` 进 scoped 会泄漏污染同名元素（本项目构建已知坑）。排查"飘浮小卡"→ `grep -rn "^\.类名" src/` 找全局裸选择器 → 看 `@include` 的 mixin 展开 → 把规则移进真正需要它的组件 scoped 块，**绝不在全局 css 用 `!important` 反打**。注意前人 `ActionList.vue:525` 这类"局部修复"往往只是半成品，需确认是否覆盖了所有受害组件。

## 本轮（最新版）settingsWindow 白屏 —— 根因定位与修复
  - `.settings-page > SETTINGSCONTENT.` 元素 `width=0 height=1004 disp=block`、内部 `.settings-content-wrapper` + `#settings-tab-panel` 全 missing
  - `contentRef.value.constructor === HTMLUnknownElement`、`Object.keys(contentRef.value) === []`
- **根因**：`SettingsPage.vue:51` 错用 `import type SettingsContent from './SettingsContent.vue'` —— type-only，运行时 SettingsContent === undefined → Vue 模板解析时找不到组件定义，把 `<SettingsContent>` 降级为 HTML 未知自定义元素 `<settingscontent>`，整个组件树从未挂载
- **修复**：去掉 `type` 关键字：`import SettingsContent from './SettingsContent.vue'`
- **本轮改动**：`src/views/settingsWindow/SettingsPage.vue`（单字符级修复）
- **验证基线**：`vue-tsc --noEmit` 0 errors；`vitest run src/views/settingsWindow` 19 文件 194 用例全绿
- **铁律已写入 `MEMORY.md`**（致命陷阱：SFC 默认导出严禁 `import type`）

## 重复界面排查结论（用户截图反馈）
- 用户截「外观设置」空白且不能返回：非重复界面问题，是 Issue 8 渲染异常空白 + 嵌入式缺返回按钮（上一轮已修 onErrorCaptured + 返回按钮）。`src/views/settingsWindow/tabs/*.vue` 17 文件无重名、SETTINGS_TABS 无重复。

## 「未检测到残留 tooltip/popover」反馈后的诊断升级（约 20:05）
- **线索**：用户点 dev 面板「扫描残留 tooltip」输出 `未检测到残留 tooltip/popover。` → 排除"浮层残留"假设；截图右侧整片黑，是真没渲染 DOM（既无 fallback，也无 NSpin）。
- **下一步**：SettingsContent 暴露 `widerProbe()`，dev 面板新增「深度探针：右栏状态」按钮。报告字段：`activeTab / renderError / headerTitle / panelLayout(wrap/header/body 三块 selector/tag/childCount/rect/bg/color/display/visibility/opacity/zIndex + text 前 120 字) / bodyChildren 前 6 个 / siblingTracks(LeftNav/Sidebar/Content/DevPanel 四轨宽度)`。
- **期望得到的诊断信号**：
  - body 的 `rect.w/h=0` 或 `display:none` → 父级 flex 链 / 父容器尺寸问题
  - body childCount=0 且 renderError=null → `<component :is>` 拿 undefined（loader 注册缺失/被隐藏）
  - body childCount=1 但 `settings-tab-wrap` rect=0×0 → 内部组件没回写高度（多根 template + flex 列）
  - siblingTracks 中 Content rect=0 但 LeftNav/Sidebar 大 → Content 被 flex 三轨挤掉
- **测试**：`SettingsContent.test.ts` 加 `exposes widerProbe() that reports panel/body/children/siblingTrack layout` 用例（happy-dom 下 mock getBoundingClientRect）；5/5 通过 · 194/194 全套。
- **遗留**：用户重启 dev → 切到任一空白 tab → 点「深度探针」→ 把 output text 完整复制回来 → 根据 childCount/rect 信号分支处置（修 wrapper 高度、Component fallback、layout flex chain）。
- **本轮改动文件**：`SettingsContent.vue`（widerProbe + elProbe）、`SettingsPage.vue`（runWideProbe + 三按钮）、`__tests__/SettingsContent.test.ts`。
- **第二段反馈（用户点「深度探针」）**：用户报告输出「探针不可用：SettingsContent 未挂载」——但 vue-tsc/test 均通过。说明 contentRef 在运行时不可达或没暴露 widerProbe。
- **升级诊断**：
  - `runWideProbe` 区分四态：`contentRef===null` / `widerProbe 不存在(打印 Object.keys)` / `执行抛错(打印 stack)` / `返回 falsy(打印值)`
  - 新增 `runGlobalProbe`：**完全绕开 contentRef**，直接扫描 `.settings-page` 树。输出 5 段：window 尺寸 / root flex 方向 / root 直属子节点（flex 三轨）/ wrapper+body / body 子节点 / contentRef 实际类型与暴露面 keys
  - 这一手能把"未挂载"原因锁死，无论 contentRef 是否能拿到
- **验证**：`vue-tsc` 0 errors；SettingsContent 5/5。
- **预期信号诊断手册**：
  - `body 没有任何子节点` → 右栏就是空的（既无 fallback 也无 component，根因在 `<component :is>` 链路）
  - `body 直属子节点只有一个 div.settings-tab-wrap 且 rect=0×0` → 组件未 mount 或没正确实例化
  - `Content 在 flex 三轨中 rect=0` → flex 链被挤掉
  - `bodyChildren 中包含 `.settings-error-fallback` 但不可见 → 异常但被容器遮挡
- **下一步**：用户重启 dev 后切到任意 tab，先点 **DOM 全局探针**（不依赖 ref，最稳定），再点 深度探针；把两段输出复制回来。
