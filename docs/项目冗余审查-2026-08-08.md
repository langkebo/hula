# 项目冗余审查清单（HuLa IM）

> 审查日期：2026-08-08
> 范围：Vue 3 + Tauri v2 + TypeScript（Matrix 协议 IM）全仓
> 方法：4 个并行 Explore 子代理按维度审查（工具函数重复 / 死代码 / 设计令牌·i18n·样式 / 组件·服务·Store 结构）
> 说明：**本清单仅列出问题，未做任何修改**。每条带 `[ ]` 勾选框，按处置优先级分组。

---

## 处置优先级总览

| 批次 | 目标 | 典型项 |
|---|---|---|
| P0 正确性/安全 | 修 Bug 与安全隐患 | `--font-family` 覆盖、HTML 转义缺 `&`、`formatDuration` 移动端缺 `Math.floor`、双校验体系冲突 |
| P1 用户可见一致性 | 统一重复实现 | 文件大小格式化、mm:ss 时长、剪贴板封装 |
| P2 高收益删死代码 | 删零引用代码 | 21 个未引用组件、`useMessageStore`、残留分支、空目录 |
| P3 结构合并 | 收敛重复组件/服务 | ThreadView、加密 flow、举报 Dialog、DM 创建、facade |
| P4 治理 | i18n/令牌/样式 | 通用文案、零引用 token、三套排版令牌、lint 盲区 |

---

## 一、死代码与未使用代码（P2，清理收益最高）

### 1.1 全仓零引用的 Vue 组件（约 3800 行）
- [x] `src/components/performance/VirtualMessageList.vue`（279 行，整个 `performance/` 目录均为死代码）
- [x] `src/components/performance/LazyImage.vue`（214 行）
- [x] `src/components/voice/VoiceRecorderEnhanced.vue`（477 行）
- [x] `src/components/search/SpotlightDialog.vue`（400 行）
- [x] `src/components/chat/ChatHistoryDrawer.vue`（276 行）
- [x] `src/components/chat/MultiMsgDrawer.vue`（185 行）
- [x] `src/components/mobile/components/chat-room/MessageContainer.vue`（262 行）
- [x] `src/components/widget/WidgetContainer.vue`（255 行）
- [x] `src/components/room/RoomListManagementDialog.vue`（234 行）
- [x] `src/components/room/PinnedMessageBanner.vue`（193 行）
- [x] `src/components/mobile/components/PullToRefresh.vue`（192 行）
- [x] `src/components/business/url-preview/UrlPreviewCard.vue`（180 行）
- [x] `src/components/business/RecallCountdown.vue`（109 行）
- [x] `src/components/common/SlidingSyncIndicator.vue`（97 行）
- [x] `src/components/rightBox/renderMessage/MessageReactions.vue`（96 行）
- [x] `src/components/quota/QuotaDisplay.vue`（90 行）
- [x] `src/components/encryption/EncryptionHealthPanel.vue`（75 行）
- [x] `src/components/mobile/components/my/MyMessageItem.vue`（55 行）
- [x] `src/components/mobile/components/virtual-scroll/MeasuredItem.vue`（9 行）
- [x] `src/components/mobile/components/my/ShareModal.vue`（7 行）
> ✅ 已删除（2026-08-08）：全部 20 个组件已删除。验证方法：排除 `typings/components*.d.ts`（自动生成）后，无任何 `.vue`/`.ts`/`.tsx` 文件通过模板标签、import 或 `defineAsyncComponent` 引用这些组件。

### 1.2 仅被自身 `__tests__` 引用的组件（需决策：待用功能 or 废弃）
- [ ] `AddFriendRequestDialog`、`AddGroupRequestDialog`、`AdminBackgroundUpdates`、`AdminExternalServices`、`ContactDetailPanel`、`DelayedEventsPanel`、`RoomReportDialog`、`RoomVaultDataPanel`、`RtcTransportsPanel`、`ThirdpartyBrowser`、`TjgRoomSummaryPanel`、`VoiceMessageEnhanced`、`DmListView`
> 若为废弃：连测试一并删除；若为待用：应补接线。

### 1.3 仅被自身测试引用的 composable（17 个，需决策）
- [ ] `useAuthErrorHandler`、`useGuestMode`、`useHeartbeat`、`useRelatedCacheInvalidation`、`useBurnAfterReadSettings`、`useAction`、`useKeyboardShortcut`、`useUploadProgress`、`useFriendGroupManagement`、`useFriendSearchView`、`usePushRulesExtended`、`useCreateRoomFlow`、`useSettingsSearch`、`useCircuitBreaker`、`useMediaCacheTTL`、`usePerformanceMonitor`、`useRetryStrategy`
> 另有 `useContextMenu.buildFriendMenu/buildRoomMenu/...`、`useFriends` 7 个排序/过滤纯函数、`useDynamic`、`webrtc/iceServers`、`sanitizeInputHtml`、`useIndependentChatWindow`、`useWsEventHandler.subscribeManagerEvents` 同为仅测试引用。

### 1.4 明确可删的死代码
- [x] `src/stores/domains/chat/message.ts`（`useMessageStore`，84 行，自述废弃仅自身测试引用）→ 连 `__tests__/message.test.ts` 删除。✅ 已删（2026-08-08）：3 个文件的类型导入重定向到 `chat/types`，`chat/__tests__/message.test.ts` 中的废弃 mock 移除。
- [x] `src/strategy/strategies/text.ts:33-37` 残留 `#replyDiv` 分支（全仓已无代码创建该节点，永远不成立，保留 `DOMPurify.sanitize` 即可）。✅ 已删（2026-08-08）
- [x] `common/matrixConstants.ts:53-93` 6 个 `_Matrix*` 私有常量零引用（极高安全）。✅ 已删（2026-08-08）：5 个 `_Matrix*` + 1 个 `_ERROR_CLIENT_NOT_INITIALIZED` 全部删除。
- [x] 10 个空目录：`src/agreement`、`src/i18n`、`src/services/location`、`src/services/robot/__tests__`、`src/services/matrix/__test_helpers__`、`src/stores/domains/chat/types`、`src/stores/domains/chat/message`、`src/stores/domains/robot`、`src/mobile/views/widget`、`src/views/announWindow`。✅ 已删（2026-08-08）

### 1.5 其他零引用符号（低）
- [ ] `src/layout/right/types.ts:33 isRightViewType` + `:20 RIGHT_VIEW_TYPES`（连带）
- [ ] `src/main.ts:84 forceUpdateMessageTop`
- [ ] `BackgroundUpdateService.ts:37 BackgroundUpdateStatus`、`MatrixMessageService.ts:31 MessageReaction` / `:38 MarkedMessage`（接口零用）
- [ ] `paths/sdk-paths.ts:25 BURN_AFTER_READ_PATHS`（疑漏测，建议补测试而非删）

---

## 二、工具函数重复（P0 正确性 / P1 一致性）

### 2.1 正确性/安全隐患（P0）
- [x] **HTML 转义 4 份字符集不一**：`Validate.ts` 的 `sanitizeInput` 漏 `&`（二次转义错乱/XSS 入口），`FriendListItem.vue` / `highlightTask.ts` 副本字符集也不全。✅ 已修（2026-08-08）：新建 `src/utils/escapeHtml.ts` 为唯一规范实现（`&`→`<`→`>`→`"`→`'` 顺序），`sanitizeInput`、`sanitizeHtml`、`FriendListItem.vue`、`highlightTask.ts` 全部改走它；`Validate.test.ts` 补 `&` 转义断言。
- [x] **双校验体系 `Validate.ts` vs `inputValidation.ts`**：MXID/RoomID/Email/URL/转义五组正则不一致导致判定冲突；`Validate.ts` 12 导出仅 2 个被用，**10 个死代码+重复** → 收敛为单体系（转义子集已随上条统一；MXID/Email/URL 校验正则收敛留待 P1，属较大重构，本轮未动以免波及众多调用方）。✅ 已修（2026-08-08）：Validate.ts 141行→8行（删 11 个死代码函数，仅保留 `validateSpecialChar`/`validateAlphaNumeric`）；inputValidation.ts 58行→15行（删 8 个死代码函数，仅保留 `isValidMatrixUserId`/`isValidMatrixRoomId`/`isNonEmptyString`）。正则不一致自动消失（重复函数已删，每个正则仅一份实现）。测试同步精简。
- [x] **`formatDuration` 桌面/移动分叉已致 BUG**：`mobile/.../BurnAfterReadSettings.vue:141` 缺 `Math.floor`（90 秒显示「1.5 分钟」）。✅ 已修（2026-08-08）：补 `Math.floor`，与桌面版 `views/settingsWindow/tabs/BurnAfterReadSettings.vue:258` 对齐。

### 2.2 用户可见一致性（P1）
- [x] **文件大小格式化 7 份**：✅ 已统一（2026-08-08）。`ImageUtils.formatFileSize` 删除并迁移调用方到 `Formatting.formatBytes`；`MatrixMultimediaService.formatFileSize`/`MemoryMonitor.vue`/`Files.vue(mobile)`/`FileUploadModal.vue(空壳)`/`QuotaDisplay.vue`/`AdminTelemetry.vue` 的本地副本全部改为引用 `Formatting.formatBytes`（单位统一 `B`、精度 `toFixed(2)`）。`ImageUtils.test.ts` 相关断言同步迁移。
- [ ] **mm:ss 时长格式化 5 份**：`Voice.vue:207`、`VoiceRecorderEnhanced.vue:186`（缺 `Math.floor`）、`useVoiceRecordRust.ts:278`、`CallView.vue:149`、`MatrixMultimediaService.ts:451`
- [x] **剪贴板复制 8 处无封装**：✅ 已统一（2026-08-08）。新建 `src/composables/common/useClipboard.ts`（Tauri 运行时优先走 `@tauri-apps/plugin-clipboard-manager`，否则回退 `navigator.clipboard`，由 `hasTauriRuntime()` 判定，单测环境走 navigator 不破坏既有断言）；`useMessageActions`/`useMessageActionHandlers`/`useMessageMultiSelect`/`Text.vue`/`InfoPopover.vue`/`EncryptionSettings.vue`/`AddFriends.vue`/`ChatSetting.vue`/`ChatMsgMultiChoose.vue` 共 9 处 `navigator.clipboard.writeText` 改为 `useClipboard().write`，并补 `useClipboard.test.ts`。`useChatCopy.ts` 本身已是 Tauri 封装，保持不动。
- [ ] **时间格式化 28 处内联绕过 `ComputedTime.ts`**：两处 `ThreadView` 逐字相同；「刚刚/X 分钟前」重复 4 处；`SearchPane.vue:310` 硬编码 `'zh-CN'`
- [ ] **MXID 解析内联 14+ 处**未走 `userIdentity.toLocalpart`/`extractServerName`（`contacts.ts` 集中 9 处 `split(':')[0]`，`MatrixContactService.ts:138` 内联 `split(':')[1]`）
- [ ] **`truncateId` 4 份（两对全等）**：`RoomEncryptedEventsPanel.vue:72`≡`RoomMessageQueuePanel.vue:63`；`RoomDetailPane.vue:287`≡`TjgRoomSummaryPanel.vue:241`
- [ ] **`checkIsUrl` 逐字重复**：`useFooterEmoji.ts:42`≡`emoticon/index.vue:365`（+`strategy/emoji.ts:13` 等价）
- [ ] **登录页 helper 各 4 份全等**：`getErrorMessage`（`forgetPasswordWindow`/`registerWindow`/`mobile/login`/`MobileForgetPassword`）、`validateMinLength`（同 4 文件）
- [ ] **会话预览截断块复制**：`useSessionListState.ts:101-108` vs `mobile/views/message/index.vue:394-401`

---

## 三、设计令牌 / i18n / 样式冗余（P4 治理）

### 3.1 设计令牌（高）
- [ ] **旧别名迁移"完毕"说法不成立**：仍 10 处用 `text-(12px --color-primary)` / `--bg-*`（如 `PinInput.vue:18`、`CheckUpdate.vue`、`registerWindow/index.vue:209`、`loginWindow/Network.vue:101`、`mobile/views/message/index.vue:197`、`ChatSetting.vue:85`）；守卫测试 `css-naming-lint.test.ts:72` 正则有盲区
- [ ] **`design-tokens.css` 140 个 token 零引用（36%）**：含向后兼容别名块 83 个中仅 3 个被消费
- [x] **`:root` 14 个同名重复定义、7 个值冲突**：`--font-family`（L316 完整字体栈 `var(--tjg-font-family)` vs L374 裸 `'PingFang'` → 后者覆盖前者，**全局丢失 `-apple-system` 回退，实为 Bug**）。✅ 已修（2026-08-08）：删除 L374 的 `--font-family: 'PingFang';` 覆盖，仅保留 `var(--tjg-font-family)` 完整回退栈。（注：`--color-primary-light`/`--color-text-quaternary` 等同名值冲突属 P4 令牌治理，不在本轮 P0 范围。）
- [ ] **三套排版令牌并行**：`--tjg-font-size-*`（247 次）/ `--font-size-*`（0 次）/ `typography.scss --text-*`（123 次），同后缀不同值（`--tjg-font-size-sm:12px` vs `--text-sm:14px`）

### 3.2 设计令牌（高，Bug）
- [x] **`--hula-*` 第三套死前缀**：`src/layout/left/model.tsx` 21 处引用但全库零定义，样式静默失效。✅ 已修（2026-08-08）：在 `design-tokens.css` 的 `:root` 补充 10 个 `--hula-*` 别名，全部映射至对应 `--tjg-*` 令牌（surface-elevated/subtle、text-primary/tertiary/quaternary/disabled、border-default、color-primary-500、color-danger-100/500），亮/暗/高对比主题下自动随 `--tjg-*` 解析。

### 3.3 i18n（中）
- [ ] **跨命名空间重复文案 1729 条（27%）**：「取消」36 处跨 21 ns、「删除」26 处跨 15 ns、「保存」15、「重试」12 → 抽 `common.json` 通用文案
- [ ] **单文件内自我重复**：zh 619 / en 583 条（`common.json` 153、`setting.json` 124）
- [ ] **989 个 key（15%）严格判定未被引用**（需复核动态拼接场景）
- [ ] 命名不一致：snake_case 4230 / camelCase 298 混用；`check-i18n.mjs` 只校验双语对齐不查重

### 3.4 样式（中）
- [ ] 重复 CSS 块 92 组：`.setting-item`(14 文件)、`.section-title`(11)、`.panel-title`(9)、`.dialog-footer`(7)
- [ ] 跨组件重复原子类组合：`flex h-40px items-center justify-center mr-12px rounded-full w-40px`(24)、`flex flex-col h-full overflow-auto`(29)
- [ ] UnoCSS 任意值双写：`[var(--tjg-x)]`(149) vs `[--tjg-x]`(1065)；11/25 快捷类零使用

---

## 四、组件 / 服务 / Store 结构冗余（P3）

- [ ] **ThreadView 桌面/移动 276 行逐字重复**（+`ThreadIndicator` 同理）→ 抽 `useThreadView()` 共享逻辑
- [ ] **加密三 flow composable 仅移动端用**：`useDeviceVerifyFlow`/`useKeyBackupFlow`/`useSecureBackupFlow`（各 190/166/166 行），桌面 3 大 Dialog（`DeviceVerifyDialog` 871 / `KeyBackupDialog` 635 / `SecureBackupDialog` 703 行）绕过重写 → 接入现有 composable 可减 1500+ 行
- [ ] **room 四 facade（Query/Member/Read/Action）零逻辑透传** → 删 Query/Member，直接用底层 service
- [ ] **DM 创建 4 处并存**：`RoomOperations.createDirectRoom` / `MatrixContactService.getOrCreateDirectChat` / `MatrixDirectMessageService.createDm/createTrustedDm` / `contacts.ts.startDirectRoom` → 收敛至 `MatrixDirectMessageService`
- [ ] **Room/User 举报 Dialog 克隆**（176/178 行，仅 props 与 service 名不同）→ 合并为参数化 `ReportDialog`
- [ ] 转发对话框 3 份（2 桌面绕过 `useMessageForward`）；密钥备份对话框 5 件套（3 处重复同一段 recovery-key 生成）
- [ ] `MatrixContactService` 三层重复包装（→ActionFacade→MembershipService）；好友三 service（`MatrixFriendService`/`MatrixContactService`/`MatrixUserDirectoryService`）边界模糊
- [ ] 两个 SummaryService 命名混淆；`group.ts` 与 `room.ts` 的 `leaveRoom` 职责交叉
- [ ] `EmptyState`/`Skeleton`/`useSpace(s)` 可顺势合并
> 不属冗余（勿合并）：`admin/RoomService` 与 `room/*` 同名方法前者走 Synapse Admin API、后者走 Client-Server API。

---

## 工具建议
- 项目已装 `knip`，但其入口仅 `src/main.ts` 且 `src/typings/components*.d.ts` 会掩盖未用组件 → 在 `knip.config.ts` 的 `ignore` 加 `src/typings/components*.d.ts` 即可复现"21 个未引用组件"。

## 验证状态
- 所有发现来自静态审查（grep + 跨文件引用核对），未运行构建/测试验证。
- 删除类操作建议先跑 `vue-tsc --noEmit` + `biome check` + 相关 `vitest` 确认无回归。
