# HuLa-Server 环境变量配置示例
# 复制此文件为 .env 并修改所有密码
# 重要: 生产环境必须修改所有默认密码!

# ==================== Nacos ====================
NACOS_VERSION=v3.0.2

# ==================== MySQL ====================
# 生成强密码: openssl rand -base64 32
MYSQL_ROOT_PASSWORD=CHANGE_ME_MYSQL_ROOT_PASSWORD
MYSQL_NACOS_PASSWORD=CHANGE_ME_NACOS_PASSWORD

# ==================== Redis ====================
# 生成强密码: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# ==================== MinIO ====================
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=CHANGE_ME_MINIO_PASSWORD

# ==================== Nacos Auth ====================
# 生成密钥: openssl rand -base64 32
NACOS_AUTH_TOKEN=CHANGE_ME_NACOS_AUTH_TOKEN
NACOS_AUTH_IDENTITY_KEY=serverIdentity
NACOS_AUTH_IDENTITY_VALUE=CHANGE_ME_IDENTITY_VALUE

# ==================== PII 加密 ====================
# 生成密钥: openssl rand -base64 32
PII_ENCRYPTION_KEY=CHANGE_ME_PII_ENCRYPTION_KEY

# ==================== SRS WebRTC ====================
# 设置为服务器公网IP
SRS_CANDIDATE=YOUR_SERVER_PUBLIC_IP

# 可选：自定义 SRS 镜像（用于无法访问 Docker Hub 的环境）
SRS_IMAGE=ossrs/srs:5

# ==================== RocketMQ ====================
# 设置为服务器IP (内网或公网)
ROCKETMQ_BROKER_IP=YOUR_SERVER_IP
